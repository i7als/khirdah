export default function Wordmark({ className = "text-5xl" }) {
  return (
    <span
      className={`${className} font-extrabold tracking-tight bg-gradient-to-l from-blue-700 via-blue-600 to-sky-400 bg-clip-text text-transparent drop-shadow-sm`}
    >
      خَرْده
    </span>
  );
}
